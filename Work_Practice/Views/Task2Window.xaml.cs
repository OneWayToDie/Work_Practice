//========================================================= Библиотеки ================================================================//
using System.Windows;              // Базовые WPF-классы (Window)

namespace Work_Practice.Views       // Пространство имён для окон и представлений
{
	//========================================================= Класс окна задания 2 ================================================================//
	public partial class Task2Window : Window
	{
		//========================================================= Конструктор окна ================================================================//
		public Task2Window()
		{
			InitializeComponent();                              // Инициализируем XAML-компоненты (загружаем разметку)
			this.Loaded += (s, e) => Helpers.WindowButtonHelper.Attach(this); // При загрузке окна подключаем кастомные кнопки управления (свернуть, развернуть, закрыть)
		}
	}
}
